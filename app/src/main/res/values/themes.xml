<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme. Compose supplies the real colors via CoolventoryTheme. -->
    <style name="Theme.Coolventory" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Splash theme using androidx core-splashscreen. Static, no heavy animation. -->
    <style name="Theme.Coolventory.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Coolventory</item>
    </style>

</resources>
